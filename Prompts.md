# 🤖 AI Prompts Used - ShopZone

1. **Architecture**: "Create a premium e-commerce layout in React with a Navbar, Shop page, and Cart page using Tailwind CSS for styling."
2. **API Integration**: "Use the DummyJSON API to fetch products and display them in a responsive grid with loading spinners."
3. **SaaS Aesthetic**: "Make the headers look minimalist with 'tracking-widest' typography and use a violet-themed color palette for a premium SaaS feel."
4. **Cart Logic**: "Implement a shopping cart using React Context API that handles adding items, removing items, and calculating the total price dynamically."
5. **The Mobile Layout**: "The product grid is breaking on small screens. Fix it so it shows 1 column on mobile, 2 on tablets, and 4 on desktop."
6. **Persistence Logic**: "My cart items disappear on refresh! Integrate `localStorage` so the cart data stays even after the browser is closed."
7. **Protected Routes**: "I need to protect the /checkout route. If a user isn't logged in, redirect them to a login page using a ProtectedRoute component."
8. **Auth Flow**: "Add a 'Login as Guest' button that updates the global authentication state and allows access to the checkout page."
9. **Error Handling**: "I am getting a fetch error in the console. Help me write a try-catch block with a fallback to show dummy products if the API is down."
10. **Bento Grid**: "Create an 'About Us' page using a Bento Box grid layout to showcase statistics and brand features with hover effects."